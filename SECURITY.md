# Security Policy

请不要在公开 issue、日志或示例中提交 Bearer credential、private key、password、数据库连接信息或生产 URL。

漏洞报告应包含可复现的最小技术描述、受影响版本和安全影响；请先通过项目维护者指定的私密渠道报告，而不是公开披露敏感细节。GOPP client 默认要求 HTTPS、TLS 校验、DNS/SSRF 防护并阻止重定向。
